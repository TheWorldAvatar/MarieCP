{
  "id": "WF_PIRMASENS_UBEM_RANKED",
  "title": "Ranked Pirmasens UBEM heat rows from cache",
  "description": "list_dabgeo_heat_supply → filter_rows → top_n_by_field",
  "city": "pirmasens",
  "parameters": {
    "city": { "type": "string", "required": true },
    "top_n": { "type": "integer", "default": 10 },
    "sort_field": { "type": "string", "default": "heat_kwh_per_m2" },
    "sort_order": { "type": "string", "default": "desc" },
    "min_heat_kwh_per_m2": { "type": "number", "default": null, "comparator": "gte", "order": 1 }
  },
  "stamp_row_columns": ["city"],
  "online_limit": 10,
  "steps": [
    {
      "id": "online_probe",
      "tool": "list_dabgeo_heat_supply",
      "args": { "city": "$city" },
      "extract": { "probe_pool": { "pick": "all_rows" } }
    },
    {
      "id": "parameter_filters",
      "type": "transform",
      "transform": "filter_rows",
      "name": "heat_threshold",
      "input_variable": "probe_pool",
      "output_variable": "rank_pool",
      "also_store_rows": "rank_pool",
      "filters": [
        { "field": "heat_kwh_per_m2", "op": "gte", "value": "$min_heat_kwh_per_m2" }
      ],
      "offline_only": true
    },
    {
      "id": "rank",
      "type": "transform",
      "transform": "top_n_by_field",
      "input_variable": "rank_pool",
      "field": "$sort_field",
      "n": "$top_n",
      "order": "$sort_order",
      "output_field": "dab_building",
      "output_variable": "top_dabgeo_iris",
      "also_store_rows": "top_ubem_rows",
      "offline_only": true
    }
  ],
  "answer_variable": "top_ubem_rows",
  "final_summary": "Top $top_n Pirmasens UBEM heat rows by $sort_field."
}
