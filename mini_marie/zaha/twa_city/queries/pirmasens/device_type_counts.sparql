PREFIX ubem: <http://www.theworldavatar.com/kg/ontoubemmp/>
SELECT ?device_type (COUNT(?dev) AS ?count)
WHERE {
  ?b a <http://www.purl.org/oema/enaeq/Building> .
  ?b ubem:hasDevice ?dev .
  ?dev a ?device_type .
}
GROUP BY ?device_type
ORDER BY DESC(?count)
